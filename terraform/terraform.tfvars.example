# Kopieer naar terraform.tfvars en pas aan (password liever via GitHub Secret):
esxi_hostname = "192.168.1.3"
esxi_username = "root"
# esxi_password = "..."  # gebruik liever repo secret TF_VAR_esxi_password

disk_store    = "datastore1"
network_name  = "VM Network"

# Je kunt desgewenst een lokaal OVF/OVA pad gebruiken:
# ovf_source = "/vmfs/volumes/datastore1/iso/ubuntu.ova"

memory        = 2048
vcpu          = 1

vm_user       = "terraformuser"
ssh_public_key = "ssh-ed25519 AAAA... jouw key ..."
